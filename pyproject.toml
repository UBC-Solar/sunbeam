[project]
name = "client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12, <3.14"
dependencies = [
    "confluent-kafka>=2.13.2",
    "influxdb-client>=1.50.0",
    "networkx>=3.6.1",
    "psycopg>=3.3.3",
    "psycopg-binary>3.1",
    "sqlalchemy>2",
    "ubc-solar-data-tools",
]

[tool.uv.sources]
ubc-solar-data-tools = { path = "../data_tools" }
